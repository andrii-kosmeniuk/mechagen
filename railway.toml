[build]
builder = "NIXPACKS"
buildCommand = "apt-get install -y openscad 2>/dev/null || true"

[deploy]
startCommand = "node backend/server.js"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
