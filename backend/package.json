{
  "name": "mechagen-backend",
  "version": "1.0.0",
  "description": "MechaGen AI geometry generation backend",
  "engines": { "node": ">=18.x" },
  "scripts": {
    "start": "vercel dev --listen 3001",
    "lint":  "node --check lib/ai.js lib/geometry.js lib/sanitize.js api/generate.js"
  },
  "devDependencies": {
    "vercel": "latest"
  }
}
